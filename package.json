{
  "name": "swagger-webmcp-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:webapi": "node scripts/generate-webapi.mjs",
    "deploy:functions": "supabase functions deploy create-order update-order-status delete-order approve-refund search-orders list-orders get-order-status update-quota",
    "predev": "npm run generate:webapi",
    "dev": "vite",
    "prebuild": "npm run generate:webapi",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.1.0"
  }
}
